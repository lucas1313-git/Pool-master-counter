node_modules/

# Regenerated by `npm run sync:ios` from the real source files (index.html,
# css/, js/, languages/, manifest.json, icons/) - never edit these copies
# directly, they're overwritten on every sync.
www/

# Xcode build noise - the ios/App project itself is committed, this is
# just the generated/local-machine cruft underneath it
ios/App/Pods/
ios/App/build/
ios/App/App/public/
*.xcuserstate
xcuserdata/
DerivedData/
